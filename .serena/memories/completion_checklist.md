After changes, run `npm run lint`, relevant tests via `npm test` or targeted smoke checks, and manual QA against the wrapper behavior because the app is user-visible and auth/proxy behavior matters. Preserve Railway requirements: /setup/healthz must stay public enough for health checks, /data remains the persistent mount, and SETUP_PASSWORD continues to protect setup/control access.